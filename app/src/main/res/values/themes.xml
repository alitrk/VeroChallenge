<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.VeroChallenge" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:textCursorDrawable">@android:color/white</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="searchHintIcon">@null</item>
    </style>


    <style name="Theme.VeroChallenge" parent="Base.Theme.VeroChallenge" />
</resources>